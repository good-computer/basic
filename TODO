parser:
 - immediate-only/program-only keywords
 - function arg type/count checks
 
memory management:
 - insert line
 - replace line
 - bounds checks on all buffers
 - garbage collector?

clearer abstractions:
- skip whitespace at start/end outside of keyword parsers?
- lift linemap->opmem loops?

execute mainloop:
- don't reset external ram position during execute if we know we're in the right spot?

functions:
  INT
  RND
  INKEY$
  LEFT$
  MID$
  RIGHT$
  TAB

keywords:
  LIST
  DIM
  READ/DATA

relops:
  AND
  OR
