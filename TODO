parser:
 - immediate-only/program-only keywords
 
memory management:
 - check program growth against var buffer and vice-versa, OOM if would overwrite

clearer abstractions:
- skip whitespace at start/end outside of keyword parsers?

functions:
  INT
  RND
  ABS
  INKEY$
  LEFT$
  MID$
  RIGHT$
  TAB

keywords:
  FOR/NEXT
  LIST
  DIM
  READ/DATA

relops:
  AND
  OR
